/**
 * Brand identity – single source of truth for design tokens.
 */
export const COLORS = {
  primary: '#231F20',
  accent: '#FF8000',
};

export const TOKENS = {
  sidebarBg: COLORS.primary,
  headerBg: COLORS.primary,
  textPrimary: COLORS.primary,
  btnPrimary: COLORS.accent,
  activeMenu: COLORS.accent,
  focusRing: COLORS.accent,
  backgroundLight: '#F8F9FA',
  surface: '#FFFFFF',
  border: '#E5E7EB',
  shadow: 'rgba(35, 31, 32, 0.08)',
};
